/**
 * Runs before the frontend test suite.
 *
 * `@testing-library/jest-dom` adds the DOM matchers (`toBeInTheDocument`,
 * `toHaveAttribute`, …) that make component assertions readable.
 */
import '@testing-library/jest-dom/vitest';
